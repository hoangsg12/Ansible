---
- name: Check Cisco Device Version
  hosts: all
  gather_facts: no
  connection: network_cli

  tasks:
    - name: Show version
      cisco.ios.ios_command:
        commands:
          - show version
      register: version_output

    - name: Print output
      ansible.builtin.debug:
        var: version_output.stdout_lines
